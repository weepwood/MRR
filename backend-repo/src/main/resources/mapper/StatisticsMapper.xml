<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zjcxph.imgapi.mapper.StatisticsMapper">

    <sql id="statisticsKeywordPredicate">
        AND (
            COALESCE(cid, '')
            || CHR(1) || COALESCE(openerno, '')
            || CHR(1) || COALESCE(date, '')
            || CHR(1) || COALESCE(type, '')
        ) LIKE '%' || #{keyword} || '%'
    </sql>

    <!-- 动态条件查询（带分页和排序） -->
    <select id="findWithConditionAndPagination" resultType="com.zjcxph.imgapi.entity.Statistics">
        SELECT * FROM mr_statistics
        <where>
            <if test="keyword != null and keyword != ''">
                <include refid="statisticsKeywordPredicate"/>
            </if>
            <if test="bah != null and bah != ''">
                AND bah LIKE '%' || #{bah} || '%'
            </if>
            <if test="sjh != null and sjh != ''">
                AND sjh LIKE '%' || #{sjh} || '%'
            </if>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
            <if test="startDate != null and startDate != ''">
                AND REPLACE(date, '/', '-') &gt;= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND REPLACE(date, '/', '-') &lt;= #{endDate}
            </if>
        </where>
        ORDER BY
        <choose>
            <when test="sortBy == 'bah'">bah</when>
            <when test="sortBy == 'cid'">cid</when>
            <when test="sortBy == 'openerNo'">openerno</when>
            <when test="sortBy == 'date'">REPLACE(date, '/', '-')</when>
            <when test="sortBy == 'type'">type</when>
            <when test="sortBy == 'pages'">pages</when>
            <when test="sortBy == 'sjh'">sjh</when>
            <otherwise>REPLACE(date, '/', '-')</otherwise>
        </choose>
        <choose>
            <when test="sortOrder == 'asc'">ASC</when>
            <otherwise>DESC</otherwise>
        </choose>
        , bah ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 根据条件统计总数 -->
    <select id="getTotalCountByCondition" resultType="java.lang.Long">
        SELECT COUNT(*) FROM mr_statistics
        <where>
            <if test="keyword != null and keyword != ''">
                <include refid="statisticsKeywordPredicate"/>
            </if>
            <if test="bah != null and bah != ''">
                AND bah LIKE '%' || #{bah} || '%'
            </if>
            <if test="sjh != null and sjh != ''">
                AND sjh LIKE '%' || #{sjh} || '%'
            </if>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
            <if test="startDate != null and startDate != ''">
                AND REPLACE(date, '/', '-') &gt;= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND REPLACE(date, '/', '-') &lt;= #{endDate}
            </if>
        </where>
    </select>

    <!-- 按日期范围和类型统计 -->
    <select id="getDateStatisticsByCondition" resultType="com.zjcxph.imgapi.dto.resp.DateStatisticsDTO">
        SELECT date, COUNT(*) as recordCount, SUM(pages) as totalPages
        FROM mr_statistics
        <where>
            <if test="startDate != null and startDate != ''">
                AND date &gt;= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND date &lt;= #{endDate}
            </if>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
        </where>
        GROUP BY date
        ORDER BY date
    </select>

</mapper>
