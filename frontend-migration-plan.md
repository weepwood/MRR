# 前端管理页面迁移计划

## 1. 目标与范围

本计划用于指导将现有 `frontend-repo` 中的管理页面，在 `frontend-fantastic-admin` 框架下重新实现，目标是：

- 完整保留现有业务功能、权限控制和关键交互流程。
- 利用 `frontend-fantastic-admin` 现有的布局、路由、菜单、标签页、主题、权限、工程化能力提升可维护性。
- 在不影响业务连续性的前提下，分阶段迁移、分批验收、可灰度上线、可快速回滚。

本次迁移的核心范围包括：

- 登录与鉴权链路
- 管理后台首页/仪表盘
- 用户管理
- 权限管理
- 日志管理
- 系统监控
- 记录管理
- 统计分析
- 测试中心
- 系统设置

建议同步纳入兼容范围的关联页面：

- `/print`
- `/:idCard`
- `/admin/:idCard/:bah`
- `/admin/statistics/detail`
- `/admin/statistics/detail/:bah`

说明：
- 这些页面虽不都属于典型后台列表页，但与现有后台导航、统计跳转、图片预览和归档流程存在直接依赖，若不纳入迁移闭环，容易出现“主页面迁完但业务链路断裂”的问题。

## 2. 现状分析

### 2.1 源项目 `frontend-repo`

当前已识别的技术特征：

- 技术栈：`Vue 3 + Vite + Pinia + Vue Router + Element Plus`
- 管理路由为手工维护，集中定义在 `src/router/index.ts`
- 业务 API 已按模块拆分，位于 `src/api`
- 业务页面主要位于 `src/views/admin`，部分复杂 UI 在 `src/components/admin`
- 已存在会话、权限判断与管理后台跳转逻辑

当前后台业务模块清单：

- `AdminDashboardPage.vue`
- `UsersPage.vue`
- `PermissionsPage.vue`
- `TestingPage.vue`
- `LogsView.vue`
- `MonitoringView.vue`
- `SettingsView.vue`
- `RecordsPage.vue`
- `StatisticsPage.vue`
- `StatisticsDetailPage.vue`
- `ArchiveImagePage.vue`

### 2.2 目标项目 `frontend-fantastic-admin`

当前已识别的框架能力：

- 技术栈升级为 `Vue 3 + TypeScript + Vite 8 + Pinia + Vue Router + UnoCSS`
- 已具备布局系统：`src/layouts`
- 已具备菜单与路由扩展能力：`src/menu`、`src/router`
- 已具备用户态与权限态容器：`src/store/modules/user.ts`
- 已具备 API 分层：`src/api/modules`
- 已预置部分业务目录：`users`、`permissions`、`logs`、`monitoring`、`records`、`settings`、`statistics`、`testing`

当前判断：

- 新框架已经完成“壳层”建设，但业务模块尚未整体迁移完成。
- `users`、`permissions` 已有初步实现，可作为迁移模板。
- 路由与菜单仍以示例内容为主，尚未形成完整后台导航结构。

## 3. 迁移原则

- 业务优先：先保证现有功能行为一致，再逐步做视觉与交互优化。
- 分层迁移：优先迁移路由、鉴权、接口、状态等基础设施，再迁业务页面。
- 双向校验：每迁完一个模块，必须与旧系统进行功能对比。
- 最小中断：通过并行开发、测试环境联调、灰度验证减少上线风险。
- 可回退：保留旧版本部署产物和回滚脚本，确保故障可在分钟级恢复。
- 统一规范：新实现统一收敛到 `frontend-fantastic-admin` 的目录、状态、路由、样式和组件规范。

## 4. 页面映射与迁移对象

| 旧项目页面 | 目标位置 | 迁移方式 | 备注 |
| --- | --- | --- | --- |
| `src/views/LoginPage.vue` | `src/views/login.vue` | 重构 | 接入新框架登录流、重定向和权限初始化 |
| `src/views/admin/AdminDashboardPage.vue` | `src/views/index.vue` 或 `src/views/dashboard/index.vue` | 重构 | 建议拆成仪表盘卡片、图表、快捷入口组件 |
| `src/views/admin/UsersPage.vue` | `src/views/users/index.vue` | 增量完善 | 以现有页面为基础继续补全行为与测试 |
| `src/views/admin/PermissionsPage.vue` | `src/views/permissions/index.vue` | 增量完善 | 保持只读/可配置边界与后端一致 |
| `src/views/admin/LogsView.vue` | `src/views/logs/index.vue` | 新建迁移 | 统一筛选、分页、详情抽屉 |
| `src/views/admin/MonitoringView.vue` | `src/views/monitoring/index.vue` | 新建迁移 | 强化仪表盘布局和轮询策略 |
| `src/views/admin/RecordsPage.vue` | `src/views/records/index.vue` | 新建迁移 | 重点处理筛选、表格、图片与详情链路 |
| `src/views/admin/StatisticsPage.vue` | `src/views/statistics/index.vue` | 新建迁移 | 统计总览与详情入口解耦 |
| `src/views/admin/StatisticsDetailPage.vue` | `src/views/statistics/detail.vue` | 新建迁移 | 需保留从统计页跳转的行为 |
| `src/views/admin/ArchiveImagePage.vue` | `src/views/statistics/archive.vue` 或 `src/views/archive-image/index.vue` | 新建迁移 | 需保留参数路由和归档图片展示 |
| `src/views/admin/TestingPage.vue` | `src/views/testing/index.vue` | 新建迁移 | 建议拆分测试工具卡片 |
| `src/views/admin/SettingsView.vue` | `src/views/settings/index.vue` | 新建迁移 | 区分系统配置、主题配置、用户偏好 |

## 5. 分阶段迁移计划

### 阶段一：需求分析与规划

目标：
- 明确业务边界、页面清单、接口清单、角色权限矩阵和验收标准。

主要工作：

- 梳理旧项目完整菜单、路由、页面、弹窗、详情页和跨页跳转关系。
- 输出页面功能清单，标注“必保留功能”“可优化功能”“可延后功能”。
- 梳理接口依赖，形成 API 映射表：
  - 接口路径
  - 请求方式
  - 请求参数
  - 返回结构
  - 异常处理
- 梳理权限矩阵：
  - 管理员
  - 普通后台用户
  - 特定权限用户
- 梳理关键业务链路：
  - 登录 -> 后台首页
  - 用户管理 -> 编辑/禁用
  - 权限查看 -> 当前账号权限确认
  - 记录查询 -> 详情/图片
  - 统计总览 -> 明细 -> 归档图像
- 定义迁移优先级：
  - P0：登录、首页、用户、权限、记录、统计
  - P1：日志、监控、设置
  - P2：测试中心、视觉统一、体验优化

交付物：

- 页面迁移清单
- API 对照表
- 权限矩阵
- 风险清单
- 里程碑排期表

建议周期：`3-4` 个工作日

### 阶段二：技术方案设计

目标：
- 完成新框架下的目录、路由、状态、接口、组件、样式、测试、部署设计。

主要工作：

- 设计目标目录结构，建议如下：

```text
src/
  api/modules/
  views/
    dashboard/
    users/
    permissions/
    logs/
    monitoring/
    records/
    statistics/
    testing/
    settings/
  components/business/
  composables/
  store/modules/
  menu/modules/
```

- 路由策略设计：
  - 使用 `frontend-fantastic-admin` 现有路由体系统一承载后台页面
  - 将后台业务路由拆分到独立模块
  - 为详情页、归档页、预览页明确动态参数规范
- 菜单策略设计：
  - 替换示例菜单
  - 以业务域组织一级/二级菜单
  - 保证菜单与权限、路由 meta 一致
- 状态管理设计：
  - 登录态、用户信息、权限集合继续集中在 Pinia
  - 页面级查询条件、表格状态尽量本地化到页面或 composable
  - 可复用的跨页面筛选逻辑抽为 composable
- API 适配设计：
  - 延续 `src/api/modules/*` 模块化封装
  - 统一请求拦截、错误处理、空态处理、消息提示
  - 明确后端字段适配层，避免页面直接依赖不稳定字段
- 组件策略设计：
  - 公共表格、搜索栏、统计卡片、详情抽屉、图片预览统一沉淀为业务组件
  - 基础 UI 优先复用 `frontend-fantastic-admin` 现有组件和布局能力
- 样式策略设计：
  - 页面骨架、间距、响应式规则统一
  - 旧项目中零散样式迁移时避免原样复制，优先收敛到框架设计规范
- 测试方案设计：
  - 单元测试：composable、格式化函数、权限判断
  - 集成测试：页面数据加载、表单编辑、路由守卫
  - E2E：登录、菜单跳转、关键业务链路

交付物：

- 技术设计说明
- 路由/菜单设计
- 组件拆分方案
- 状态与 API 设计
- 测试策略文档

建议周期：`3` 个工作日

### 阶段三：页面组件迁移实施

目标：
- 按业务域逐模块完成重构迁移，确保每个模块具备独立可联调、可验收能力。

实施顺序建议：

1. 基础设施迁移
2. 登录与权限体系迁移
3. 首页与菜单迁移
4. 用户/权限模块迁移
5. 记录/统计模块迁移
6. 日志/监控/设置迁移
7. 测试中心与附属页面迁移

#### 3.1 基础设施子任务

- 建立正式业务菜单模块，替换示例菜单
- 建立后台业务路由模块
- 统一 `layout + tabbar + breadcrumb + keepAlive` 策略
- 统一环境变量与接口地址配置
- 统一登录失效、无权限、空数据、异常提示体验

#### 3.2 登录与权限子任务

- 对齐旧项目 `session`、`isAdminUser`、`hasAnyPermission` 行为
- 完善 `frontend-fantastic-admin` 中 `user store` 的权限拉取与初始化逻辑
- 保证以下行为一致：
  - 未登录跳转登录页
  - 已登录访问登录页自动跳转
  - 无权限用户不能进入后台业务页
  - 页面按钮/菜单根据权限控制显示

#### 3.3 页面迁移子任务

用户管理：

- 保留用户查询、角色展示、状态过滤、编辑、启停用能力
- 补齐表单校验、异常提示、加载态、空态
- 补充测试数据与回归脚本

权限管理：

- 保留角色与权限点查看能力
- 保留当前账号权限可视化
- 如后端仍为只读模型，则页面继续只读实现

记录管理：

- 迁移搜索条件、表格、分页、详情、图片入口
- 抽离查询区和列表区，减少巨型页面耦合
- 为复杂筛选保留状态记忆能力

统计分析：

- 迁移总览、明细、详情跳转、按条件过滤
- 保留跳转到归档图片页的深链能力
- 将统计卡片和图表封装为可复用组件

日志管理：

- 迁移日志表格、条件筛选、详情查看
- 对大数据量日志查询增加防抖、分页与懒加载策略

系统监控：

- 迁移监控卡片、系统指标展示
- 评估轮询频率，避免无效请求造成页面负担

系统设置：

- 迁移系统参数配置、开关项、基础设置展示
- 将系统配置与用户偏好设置分层管理

测试中心：

- 迁移原测试工具页面
- 区分“内部调试能力”和“正式生产功能”，避免误入主业务导航

交付要求：

- 每个模块迁移完成后必须通过：
  - 自测
  - 代码评审
  - 联调确认
  - 回归清单勾选

建议周期：`10-15` 个工作日

### 阶段四：功能测试与调试

目标：
- 确保迁移后功能完整、行为一致、异常可控。

测试内容：

- 功能测试
  - 页面加载
  - 列表查询
  - 表单编辑
  - 路由跳转
  - 权限控制
  - 异常处理
- 回归测试
  - 对照旧系统逐项验证核心业务
  - 验证旧链接、深链、动态参数页面
- 联调测试
  - 与后端接口联调
  - 校验异常码、空数据、边界值
- 调试修复
  - 统一修复字段映射问题
  - 统一修复时区/日期格式问题
  - 统一修复上传/预览/下载等浏览器差异问题

测试出口条件：

- P0 模块无阻断缺陷
- P1 模块无高严重度缺陷
- 所有核心路径通过回归

建议周期：`5-7` 个工作日

### 阶段五：性能优化及上线部署

目标：
- 达成性能指标并完成生产发布、灰度验证与上线保障。

性能优化重点：

- 路由级懒加载
- 重型页面拆分子组件，降低首屏 JS 体积
- 大表格按需渲染、分页加载
- 筛选请求防抖与取消重复请求
- 图片资源压缩与预览懒加载
- 公共依赖收敛，减少重复打包
- 使用框架现有构建压缩能力生成更优产物

上线实施步骤：

1. 构建测试环境发布包并完成 UAT 验收
2. 生成生产构建产物并执行 smoke test
3. 进行灰度发布
4. 观察错误日志、接口耗时、页面打开成功率
5. 放量全量切换
6. 保留旧版回滚入口至少一个发布周期

部署与回滚要求：

- 保留旧版静态资源包
- 保留旧版环境变量与部署脚本
- 回滚时只切换前端静态资源版本，不改后端接口契约

建议周期：`2-3` 个工作日

## 6. 推荐里程碑与排期

| 里程碑 | 内容 | 建议耗时 |
| --- | --- | --- |
| M1 | 需求分析完成，范围冻结 | 3-4 天 |
| M2 | 技术方案与路由/菜单设计完成 | 3 天 |
| M3 | 登录、首页、用户、权限模块完成 | 5-6 天 |
| M4 | 记录、统计模块完成 | 4-5 天 |
| M5 | 日志、监控、设置、测试中心完成 | 4-5 天 |
| M6 | 全量测试与性能优化完成 | 5-7 天 |
| M7 | 灰度上线与正式发布 | 2-3 天 |

总周期建议：`26-33` 个工作日  
若采用 2 人并行开发，可压缩至 `18-24` 个工作日。

## 7. 测试计划

### 7.1 功能测试

必须覆盖：

- 登录、退出、登录态恢复
- 菜单展示与权限过滤
- 页面查询、重置、分页、排序
- 新增、编辑、启停用、只读查看
- 图表展示与详情跳转
- 图片预览、归档明细跳转
- 网络异常、空数据、超时、权限不足

### 7.2 兼容性测试

主流浏览器验证范围：

- Chrome 最新稳定版
- Edge 最新稳定版
- Firefox 最新稳定版
- Safari 最新稳定版

兼容性验证项：

- 页面布局完整性
- 表单控件交互一致性
- 表格滚动与固定列表现
- 图像预览与下载能力
- 字体、图标、动画降级行为

### 7.3 性能测试

建议设置如下验收指标：

- 登录后后台首页首屏可交互时间 `<= 3s`
- 常规业务页切换时间 `<= 1.5s`
- 列表查询接口响应后页面渲染完成时间 `<= 2s`
- 首屏 JS 体积较旧版下降 `20%+`
- 大表格交互卡顿显著降低，滚动保持流畅

建议监控指标：

- LCP
- INP
- CLS
- 首屏资源大小
- 页面请求数
- 错误率

## 8. 风险与应对

### 8.1 主要风险

- 旧页面逻辑分散在 `views` 与 `components/admin`，迁移时容易遗漏隐式依赖
- 新旧路由模式不同，可能导致跳转、深链和权限守卫行为不一致
- 后端返回字段不稳定或缺少权限元数据，影响页面统一建模
- 统计、记录、图片相关页面逻辑复杂，容易出现链路断点
- 浏览器兼容与表格性能问题可能在后期才暴露

### 8.2 应对策略

- 建立“旧页面 -> 新页面 -> 验收结果”对照表
- 每个业务域迁移前先输出字段映射和流程图
- 为高风险模块优先做联调和冒烟测试
- 引入关键链路 E2E 用例，避免回归遗漏
- 上线前做一次真实数据压测和浏览器回归

## 9. 资源建议

建议角色配置：

- 前端开发 `2` 人
- 测试 `1` 人
- 产品/业务验收 `1` 人
- 后端接口支持 `1` 人

并行分工建议：

- A 线：登录、权限、菜单、用户、权限模块
- B 线：记录、统计、日志、监控、设置模块

## 10. 迁移完成定义

满足以下条件方可视为迁移完成：

- 所有现有业务功能已在 `frontend-fantastic-admin` 中可用
- 与旧系统对照后的 P0/P1 功能无缺失
- 主流浏览器兼容性测试通过
- 性能测试达到预设指标
- 灰度阶段无阻断问题
- 上线与回滚方案已验证

## 11. 建议的首批落地动作

为尽快进入实施，建议下一步按以下顺序推进：

1. 冻结最终页面清单与权限矩阵
2. 完成 `frontend-fantastic-admin` 正式业务菜单与路由结构
3. 统一登录态、权限守卫和 API 错误处理
4. 以 `Users`、`Permissions` 作为模板模块完成规范沉淀
5. 优先迁移 `Records` 与 `Statistics` 两个高价值且高风险模块
6. 建立回归清单、浏览器矩阵和性能基线

---

该计划适合作为迁移项目的主执行文档。若继续推进，建议下一步补充两份配套文档：

- `页面/接口迁移对照表`
- `测试用例与验收清单`
